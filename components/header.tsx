'use client';
import Link from 'next/link'; import { Menu, X } from 'lucide-react'; import { useState } from 'react';
export function Header(){const [open,setOpen]=useState(false);const links=[['Find a ride','/find'],['Offer a ride','/offer'],['Safety','/about'],['Help','/rules']];return <header className="site-header"><div className="shell nav-wrap"><Link href="/" className="brand" aria-label="Ride With Me home"><span className="brand-mark">R</span><span>Ride <b>With Me</b></span></Link><nav className="desktop-nav">{links.map(([n,h])=><Link href={h} key={h}>{n}</Link>)}</nav><div className="nav-actions"><Link href="/login" className="login-link">Log in</Link><Link href="/signup" className="ride-btn ride-btn-dark">Get started</Link></div><button onClick={()=>setOpen(!open)} className="menu-button" aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></div>{open&&<div className="mobile-nav shell">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} href={h} key={h}>{n}</Link>)}<Link href="/login">Log in</Link><Link href="/signup" className="ride-btn ride-btn-primary">Get started</Link></div>}</header>}
