'use client';
import Link from 'next/link'; import { Menu, X } from 'lucide-react'; import { useState } from 'react';
export function Header(){const [open,setOpen]=useState(false);const links=[['Explore','/explore'],['Leaderboard','/leaderboard'],['How it works','/about'],['Rules','/rules']];return <header className="sticky top-0 z-30 border-b bg-white/80 backdrop-blur"><div className="shell flex h-17 items-center justify-between py-3"><Link href="/" className="text-xl font-black tracking-tight">prom<span className="text-violet-700">ova</span></Link><nav className="hidden items-center gap-6 md:flex">{links.map(([n,h])=><Link className="text-sm font-medium text-slate-600 hover:text-violet-700" href={h} key={n}>{n}</Link>)}</nav><div className="hidden gap-2 md:flex"><Link href="/login" className="btn-secondary px-4 py-2">Log in</Link><Link href="/signup" className="btn-primary px-4 py-2">Get started</Link></div><button onClick={()=>setOpen(!open)} className="rounded-lg p-2 md:hidden" aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t bg-white p-5 md:hidden"><nav className="shell grid gap-4">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} href={h} key={n}>{n}</Link>)}<Link href="/login">Log in</Link><Link href="/signup" className="btn-primary">Get started</Link></nav></div>}</header>}
