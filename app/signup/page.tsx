'use client'; import Link from 'next/link'; import { useState } from 'react'; import { createClient } from '@supabase/supabase-js';
export default function Signup(){const [message,setMessage]=useState('');async function submit(f:FormData){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMessage('Add Supabase environment variables to create an account.');return}const s=createClient(url,key);const {error}=await s.auth.signUp({email:String(f.get('email')),password:String(f.get('password')),options:{data:{role:f.get('role')},emailRedirectTo:`${location.origin}/auth/callback`}});setMessage(error?error.message:'Check your email to verify your account.')}return <main className="shell grid min-h-[75vh] place-items-center py-12"><form action={submit} className="card w-full max-w-md"><p className="eyebrow">Join Promova</p><h1 className="mt-2 text-3xl font-black">Build your audience.</h1><label className="mt-7 block text-sm font-medium">Email<input required name="email" type="email" className="input mt-2"/></label><label className="mt-4 block text-sm font-medium">Password<input required minLength={8} name="password" type="password" className="input mt-2"/></label><label className="mt-4 block text-sm font-medium">I’m here as<select name="role" className="input mt-2"><option value="creator">A creator</option><option value="brand">A brand</option><option value="viewer">A viewer</option></select></label><button className="btn-primary mt-6 w-full">Create account</button>{message&&<p className="mt-4 text-sm text-slate-600">{message}</p>}<p className="mt-6 text-center text-sm text-slate-600">Already a member? <Link className="font-semibold text-violet-700" href="/login">Log in</Link></p></form></main>}
