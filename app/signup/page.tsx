'use client'; import Link from 'next/link'; import { useState } from 'react'; import { createClient } from '@supabase/supabase-js';
export default function Signup(){const [message,setMessage]=useState('');async function submit(f:FormData){const url=process.env.NEXT_PUBLIC_SUPABASE_URL || 'https://kxjyewhhdyeylfagwieb.supabase.co',key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || 'sb_publishable_qekJJhIYjEh52w5IfnGCqw_LRraeFBG';if(!url||!key){setMessage('Add Supabase environment variables to create an account.');return}const s=createClient(url,key);const redirectBase = typeof window !== 'undefined' && window.location.hostname !== 'localhost' ? window.location.origin : (process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000');const {error}=await s.auth.signUp({email:String(f.get('email')),password:String(f.get('password')),options:{data:{role:f.get('role')},emailRedirectTo:`${redirectBase}/auth/callback`}});setMessage(error?error.message:'Check your email to verify your account.')}return <main className="finder-page"><div className="shell finder"><form action={submit} className="find-card"><p className="kicker">JOIN THE NETWORK</p><h1>Create your account</h1><label><span>EMAIL</span><div className="find-input"><input required name="email" type="email"/></div></label><label className="block mt-4"><span>PASSWORD</span><div className="find-input"><input required minLength={8} name="password" type="password"/></div></label><label className="block mt-4"><span>I’M HERE AS</span><div className="find-input"><select name="role"><option value="viewer">A passenger</option><option value="creator">A driver</option></select></div></label><button className="search-button">Create account</button>{message&&<p className="form-note">{message}</p>}<p className="mt-6 text-center text-sm">Already a member? <Link href="/login">Log in</Link></p></form></div></main>}
