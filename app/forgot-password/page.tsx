'use client'; import { useState } from 'react'; import { createClient } from '@supabase/supabase-js';
export default function Forgot(){const [message,setMessage]=useState('');async function submit(f:FormData){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!u||!k)return setMessage('Add Supabase environment variables first.');const {error}=await createClient(u,k).auth.resetPasswordForEmail(String(f.get('email')),{redirectTo:`${location.origin}/reset-password`});setMessage(error?error.message:'If an account exists, a reset link is on its way.')}return <main className="shell grid min-h-[75vh] place-items-center"><form action={submit} className="card w-full max-w-md"><h1 className="text-2xl font-black">Reset password</h1><p className="mt-2 text-sm text-slate-600">We’ll send a secure reset link.</p><input name="email" required type="email" className="input mt-6" placeholder="you@example.com"/><button className="btn-primary mt-4 w-full">Send reset link</button>{message&&<p className="mt-4 text-sm">{message}</p>}</form></main>}
