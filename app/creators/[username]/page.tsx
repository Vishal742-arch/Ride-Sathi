import { notFound } from 'next/navigation'; import { creators } from '@/components/creator-card'; import { BadgeCheck, ExternalLink, Heart, Eye } from 'lucide-react';
export default async function Profile({params}:{params:Promise<{username:string}>}){const {username}=await params;const c=creators.find(x=>x.username===username);if(!c)notFound();return <main className="shell py-12"><div className="grid gap-9 lg:grid-cols-[260px_1fr]"><img className="h-64 w-full rounded-3xl object-cover" src={c.image} alt=""/><section><div className="flex items-center gap-2"><h1 className="text-4xl font-black">{c.name}</h1>{c.verified&&<BadgeCheck className="text-violet-600" fill="currentColor"/>}</div><p className="mt-2 text-violet-700">@{c.username}</p><p className="mt-6 max-w-2xl leading-7 text-slate-600">Making useful, beautiful things for a curious community. Follow along for fresh work, honest recommendations, and a little inspiration.</p><div className="mt-7 grid grid-cols-3 gap-3">{[[c.followers,'Followers'],[c.views,'Total views'],[c.engagement,'Engagement']].map(([n,l])=><div className="card p-4" key={l}><b className="text-xl">{n}</b><p className="mt-1 text-xs text-slate-500">{l}</p></div>)}</div><a className="btn-primary mt-7" href="#portfolio">Visit portfolio <ExternalLink className="ml-2" size={16}/></a></section></div><section id="portfolio" className="mt-16"><p className="eyebrow">Promoted content</p><h2 className="mt-2 text-2xl font-bold">Latest from {c.name.split(' ')[0]}</h2><div className="card mt-6 flex items-center justify-between"><div><b>A thoughtful new story from {c.name}</b><p className="mt-1 text-sm text-slate-500">Promoted · {c.category}</p></div><div className="flex gap-4 text-sm text-slate-500"><span className="flex gap-1"><Eye size={16}/>{c.views}</span><span className="flex gap-1"><Heart size={16}/>{c.engagement}</span></div></div></section></main>}
