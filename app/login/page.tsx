'use client'; import Link from 'next/link'; import { useState } from 'react'; import { createClient } from '@supabase/supabase-js';
export default function Login(){const [message,setMessage]=useState('');async function submit(f:FormData){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMessage('Add Supabase environment variables to enable sign in.');return}const s=createClient(url,key);const {error}=await s.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});setMessage(error?error.message:'Signed in. Redirecting…');if(!error)location.href='/dashboard'}return <main className="shell grid min-h-[75vh] place-items-center py-12"><form action={submit} className="card w-full max-w-md"><p className="eyebrow">Welcome back</p><h1 className="mt-2 text-3xl font-black">Log in to Promova</h1><label className="mt-7 block text-sm font-medium">Email<input required name="email" type="email" className="input mt-2"/></label><label className="mt-4 block text-sm font-medium">Password<input required name="password" type="password" className="input mt-2"/></label><button className="btn-primary mt-6 w-full">Log in</button>{message&&<p className="mt-4 text-sm text-slate-600">{message}</p>}<div className="mt-6 flex justify-between text-sm text-violet-700"><Link href="/forgot-password">Forgot password?</Link><Link href="/signup">Create account</Link></div></form></main>}
