'use client'; import { useMemo, useState } from 'react'; import { Search, SlidersHorizontal } from 'lucide-react'; import { CreatorCard, creators } from '@/components/creator-card';
export default function Explore(){const [q,setQ]=useState('');const [category,setCategory]=useState('All');const categories=['All','Beauty & Style','Food & Culture','Photography','Tech & Design'];const items=useMemo(()=>creators.filter(c=>(category==='All'||c.category===category)&&`${c.name} ${c.category}`.toLowerCase().includes(q.toLowerCase())),[q,category]);return <main className="shell py-14"><p className="eyebrow">Marketplace</p><h1 className="mt-2 text-4xl font-black">Discover your next favorite.</h1><p className="mt-3 max-w-xl text-slate-600">Explore promoted work from ambitious, original creators.</p><div className="mt-9 flex flex-col gap-4 rounded-2xl border bg-white p-4 sm:flex-row"><label className="flex flex-1 items-center gap-2 rounded-xl bg-slate-50 px-3"><Search size={18} className="text-slate-400"/><input className="w-full bg-transparent py-2 outline-none" placeholder="Search creators or categories" value={q} onChange={e=>setQ(e.target.value)}/></label><select value={category} onChange={e=>setCategory(e.target.value)} className="rounded-xl border px-3 py-2 text-sm">{categories.map(x=><option key={x}>{x}</option>)}</select></div><div className="mt-7 flex flex-wrap gap-2">{['Trending','Newest','Most viewed','Most engaged'].map((x,i)=><button className={`rounded-full px-4 py-2 text-sm ${i===0?'bg-violet-700 text-white':'bg-white ring-1 ring-slate-200'}`} key={x}>{x}</button>)}</div><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map(c=><CreatorCard creator={c} key={c.username}/>)}</div>{!items.length&&<div className="py-20 text-center text-slate-500">No creators match that search.</div>}</main>}
