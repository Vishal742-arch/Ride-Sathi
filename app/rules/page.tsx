'use client';
import { useState } from 'react';
import { AlertTriangle, ArrowRight, CheckCircle2, ShieldAlert } from 'lucide-react';
const rules=['Use your real identity and keep your profile information current.','Respect passengers, drivers and local traffic rules throughout a trip.','Do not share private contact details or arrange payment outside the platform.','Only use a vehicle that is approved for the published ride.','Cancel early if your plans change; repeated no-shows may limit account access.','Report unsafe behaviour, payment problems or incorrect vehicle details promptly.'];
export default function Rules(){const [sent,setSent]=useState(false);return <main className="info-page"><section className="shell info-hero compact"><span className="kicker">HELP & SAFETY</span><h1>Good rides start with <em>good conduct.</em></h1><p>Simple expectations help everyone travel more comfortably and safely.</p></section><section className="shell rules-layout"><div className="rules-list">{rules.map((rule,i)=><article key={rule}><span>0{i+1}</span><p>{rule}</p><CheckCircle2 size={20}/></article>)}</div><form className="report-card" onSubmit={e=>{e.preventDefault();setSent(true)}}><ShieldAlert size={28}/><h2>Report a ride issue</h2><p>For an immediate danger, use your local emergency number first. This form sends a report to the Ride With Me support workflow.</p><label>Category<select required defaultValue=""><option value="" disabled>Choose an issue</option><option>Safety concern</option><option>Driver or passenger conduct</option><option>Vehicle details</option><option>Payment or booking</option><option>Other</option></select></label><label>What happened?<textarea required placeholder="Share only the information needed to investigate." /></label><button className="ride-btn ride-btn-primary">Send report <ArrowRight size={17}/></button>{sent&&<p className="report-sent"><AlertTriangle size={16}/> Report recorded for support follow-up.</p>}</form></section></main>}
